@Content

@code {
    /// <summary>
    /// INTERNAL USE ONLY: This component is used internally by FlowyCanvasEditor for HTML rendering.
    /// Do not use this component directly in your application.
    /// </summary>
    [Parameter]
    public RenderFragment? Content { get; set; }
}
